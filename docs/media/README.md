# Demo media（GitHub 展示）

| 文件 | 内容 |
|------|------|
| `sim_flip_v2ft.mp4` / `.gif` | MuJoCo **v2-ft 三模态**翻方块（外置机位 + 腕部 RGB）；录到方块彻底翻倒并短暂收尾 |
| `virtual_target_stiffness_demo.png` | 阶段一核心算法 Demo 静态图（虚拟目标 + 变刚度） |

重新生成翻方块录屏：

```bash
bash scripts/make_github_media.sh
```
